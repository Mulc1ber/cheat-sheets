import type { ReactNode } from "react";
import Layout from "@theme/Layout";

export default function ReactPage(): ReactNode {
  return (
    <Layout>
      <h1>My React page</h1>
      <p>This is a React page</p>
    </Layout>
  );
}
